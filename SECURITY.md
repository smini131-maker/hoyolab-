# 보안 정책과 위협 모델

## 보호 대상

가장 민감한 자산은 HoYoLAB 세션 쿠키입니다. 계정 비밀번호는 앱 입력창이나 네이티브 코드로 받지 않습니다. 출석 성공 날짜, 실행 시각, 결과 종류는 민감도가 낮은 로컬 상태로 취급합니다.

## 설계

- 로그인 화면은 `FLAG_SECURE`를 사용하며 공식 `hoyolab.com`/`hoyoverse.com` HTTPS 호스트만 WebView 내부에서 엽니다.
- JavaScript와 DOM 저장소는 공식 로그인에 필요한 동안만 켭니다. 파일/콘텐츠/위치/혼합 콘텐츠 접근은 끕니다.
- 필요한 인증 쿠키 이름만 허용 목록으로 추출합니다. 분석·테마 등 무관한 쿠키는 버립니다.
- 쿠키는 Android Keystore에서 생성한 256비트 AES 키와 `AES/GCM/NoPadding`, 매 저장마다 새로운 12바이트 IV, 128비트 인증 태그로 암호화합니다.
- 앱 백업과 cleartext 트래픽을 비활성화했습니다. 신뢰 앵커는 시스템 CA만 사용합니다.
- WebView가 닫힐 때 내부 쿠키, 기록, 클라이언트와 뷰를 제거합니다. 앱 프로세스가 로그인 도중 비정상 종료되면 WebView 앱 샌드박스에 임시 쿠키가 남을 수 있으므로 다음 로그인 전에 앱 데이터 삭제를 선택할 수 있습니다.
- 쿠키/요청 헤더/응답 원문을 로그로 남기지 않습니다. 디버그 로그 유틸리티도 토큰, 쿠키, 이메일, 긴 숫자 ID를 마스킹합니다.
- 출석 요청은 `HttpsURLConnection`으로만 보내며 5초 연결/읽기 제한, 64KiB 응답 상한, 리디렉션 차단을 적용합니다.
- 로그아웃은 암호문 SharedPreferences와 Keystore 키를 모두 삭제합니다.

## 자동화 안전 경계

- CAPTCHA, `gt/challenge`, 위험 코드가 있으면 즉시 중단하고 공식 페이지에서 사용자 확인을 요구합니다.
- 인증 오류는 재시도하지 않습니다.
- 429 및 일시적 네트워크/5xx만 WorkManager 지수 백오프로 최대 2회 재시도합니다.
- 서버 성공 날짜를 우선 저장하고, 서버 날짜가 없을 때만 UTC+8 로컬 정책을 사용합니다.
- 동일 날짜 성공 기록이 유효하면 네트워크 요청 0회로 종료합니다. 기기 시각/시간대 변경 후에는 서버를 한 번 다시 확인합니다.
- 앱 자체 Service, 포그라운드 Worker, 정확 알람, 무한 루프, 폴링, 분석 SDK가 없습니다. WorkManager 내부 JobService 구성요소는 Android 예약 실행에만 사용됩니다.

## 알려진 한계

- HoYoLAB 웹 API는 공식 공개 계약이 아니며 주소, 쿠키 이름, 이벤트 ID, 응답 코드가 예고 없이 바뀔 수 있습니다.
- 시스템 CA 기반 TLS를 사용하고 인증서 고정은 하지 않습니다. 인증서 순환 대응성과 사용자 기기 정책을 유지하기 위한 선택입니다.
- Android 앱 샌드박스, 기기 잠금/FBE, 최신 WebView와 OS 보안 업데이트가 전제입니다. 루팅·후킹·악성 접근성 서비스가 있는 기기는 보호 범위 밖입니다.
- 디버그 APK는 공개 Android 디버그 키로 서명되므로 신뢰 배포용이 아닙니다. 프로덕션 릴리스는 별도 안전한 키로 서명해야 합니다.

## 보안 검증

- AES-GCM 정상 복호화와 잘못된 키 실패 테스트
- 쿠키 허용 목록과 불필요 쿠키 제거 테스트
- 로그 마스킹 테스트
- HTTPS 전용/cleartext 금지/백업 금지/비공개 컴포넌트 정적 점검
- APK DEX에서 실제 `ltoken=`, `cookie_token=`, `Bearer` 값 패턴 검색
- Keystore 저장/삭제 계측 테스트 소스와 계측 APK 포함

실기기 계측 결과는 아직 없으며 [docs/TEST_REPORT.md](docs/TEST_REPORT.md)에 미검증으로 표시했습니다.

## 취약점 제보

실제 인증정보, 쿠키, 계정 ID, 이메일 또는 로그 원문을 공개 이슈에 붙이지 마세요. 재현 가능한 최소 정보와 영향만 제공하고, 저장소 소유자에게 비공개 보안 제보 경로를 먼저 요청하세요.
